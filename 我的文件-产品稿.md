# 飞象智能作业｜我的文件产品稿

> 版本：v1.0  
> 适用端：教师端「作业 > 我的资源 > 我的文件」  
> 状态：Demo 对齐稿

## 1. 功能说明

「我的文件」用于教师集中管理本地教学资料，并将**单个文件**发布至学生墨水屏设备。

本期包括四个能力：

1. 文件夹与文件管理：在根目录或任意文件夹浏览、搜索、创建文件夹、移动、重命名、删除文件。
2. 上传文件：支持单文件、多个文件和文件夹上传；上传后资料保存在当前目录。
3. 文件预览与下载：文档、音频、视频均可进入文件详情页预览，支持下载原文件。
4. 发布到墨水屏：教师选择学科与接收对象后发布；同一文件可多次发布给不同班级或学生，并在独立的发布记录中追溯和删除。

## 2. 目标与范围

### 2.1 解决的问题

- 教师需要按教学主题或年级整理资料，并在需要时快速发给学生设备。
- 同一份资料可能多次发放，且每次接收范围不同；教师需要知道“发给了谁”，并可撤回某一次发放。
- 发布前需要明确学科与接收对象，避免资料错发、漏发或无法追溯。

### 2.2 本期范围

- 教师端文件夹管理、上传、预览、下载。
- 单个文件发布至墨水屏。
- 班级 / 指定学生两种接收对象展示。
- 发布记录查看与删除同步。

### 2.3 不在本期范围

- 文件夹整体发布至墨水屏。
- 文件详情页展示发布记录入口；发布记录只在文件列表的独立入口查看。
- 文件内容 AI 解读、问答、分享等附加模块。
- 学生端对文件的编辑、再次转发或上传。

## 3. 角色与权限

| 角色 | 权限 |
| --- | --- |
| 教师 | 浏览和管理本人可见文件；上传、预览、下载；发布单个文件；查看并删除本人发起的发布记录。 |
| 学生 | 在墨水屏设备查看被发布且未被删除的文件。 |

## 4. 页面与交互流程

### 4.1 文件列表

1. 教师从「我的资源」进入「我的文件」。
2. 页面默认展示当前目录下的文件夹与文件，支持通过面包屑返回上级或根目录。
3. 顶部提供搜索、发布记录、管理、新建文件夹、上传文件入口。
4. 点击文件夹进入下级目录；点击单个文件进入文件详情页；管理模式下不进入详情页。

### 4.2 上传文件

1. 教师在根目录或当前文件夹点击「上传文件」。
2. 弹窗支持拖入文件，或选择文件 / 文件夹；一次可加入多个文件。
3. 已选择状态展示文件数、文件夹数、总大小及文件清单；不支持或超出大小限制的文件单独提示。
4. 点击「开始上传」后，仅展示上传进度、当前上传文件和已上传数量。
5. 上传完成后提示“文件已入库”；点击完成返回当前目录。
6. 上传中关闭弹窗时，二次确认是否退出；已上传文件保留，未完成文件停止上传。

### 4.3 文件详情页

1. 页面展示文件类型、文件名、上传人、更新时间和大小。
2. 文档展示原文件预览；音频、视频展示对应的播放预览区域。
3. 页面仅提供「发布到墨水屏」和「下载」操作，**不展示发布记录**。

### 4.4 发布到墨水屏

1. 教师从文件列表的更多操作，或文件详情页点击「发布到墨水屏」。
2. 弹窗展示待发布文件，教师必须选择学科和接收学生。
3. 点击选择学生后，在选择器内按教学班 / 行政班浏览、搜索、勾选学生，并展示已选人数。
4. 选择全部班级学生时，本次发布按“班级”保存；选择部分学生时，按“学生”保存。
5. 点击「确认发布」后生成一条独立发布记录；同一文件允许再次以新的接收对象发布。

### 4.5 发布记录与删除

1. 教师从文件列表顶部进入「发布记录」。
2. 每条记录展示发布文件、学科、接收对象、发布时间、状态和操作。
3. 接收对象为整班时展示班级名称；接收对象为部分人员时展示学生姓名。
4. 教师可查看本次接收班级或接收学生。
5. 点击删除后弹出确认框，操作按钮为「取消 / 确认」；确认删除后，该次发布记录移除，学生设备同步不再显示该文件。

## 5. 业务规则

### 5.1 文件与上传规则

| 项目 | 规则 |
| --- | --- |
| 支持类型 | 文档、图片、音频、视频。 |
| 文档 / 图片大小 | 单个文件不超过 100MB。 |
| 音频 / 视频大小 | 单个文件不超过 300MB。 |
| 上传位置 | 根目录与任意文件夹均可上传；上传结果保存在当前目录。 |
| 多文件 | 一次可选择多个文件，也可选择整个文件夹。 |
| 不支持文件 | 不进入上传结果；在待上传清单中标记原因。 |

### 5.2 发布规则

| 项目 | 规则 |
| --- | --- |
| 发布对象 | 仅单个文件可发布；文件夹不可发布到墨水屏。 |
| 必填项 | 学科、接收学生均为必填；未完整选择时不可确认发布。 |
| 多次发布 | 同一文件可多次发布；每次发布独立生成记录。 |
| 审核限制 | 审核中或审核失败的文件不可发布，操作区提示原因。 |
| 整班识别 | 选择某班全部学生时，记录按班级展示；否则按学生展示。 |
| 删除发布 | 删除只影响对应的一次发布，不删除教师端原文件，也不影响该文件的其他发布记录。 |
| 学生端同步 | 删除发布确认后，学生设备不再显示该次发布的文件。 |

## 6. 数据说明

### 6.1 文件列表

| 展示项 | 说明 |
| --- | --- |
| 文件名 | 文件或文件夹名称。 |
| 类型图标 | 通过文件类型区分文档、音频、视频等。 |
| 上传者 | 当前展示文件的上传教师。 |
| 更新时间 | 文件最近上传或更新的时间。 |
| 大小 | 单文件大小；文件夹显示“-”。 |
| 审核状态 | 审核中、审核失败时展示状态标签并限制发布。 |

### 6.2 发布记录

| 展示项 | 说明 |
| --- | --- |
| 发布文件 | 被发布的单个文件名称。 |
| 学科 | 教师发布时选择的学科。 |
| 接收对象 | 整班显示班级名称；指定人员显示学生姓名。 |
| 发布时间 | 本次发布创建时间。 |
| 状态 | 当前为“已发布”。 |
| 操作 | 查看接收对象、删除本次发布。 |

## 7. 异常场景

| 触发 | 页面反馈 | 恢复方式 |
| --- | --- | --- |
| 未选择文件即开始上传 | 开始上传不可用。 | 选择至少一个符合规则的文件。 |
| 文件格式不支持或超过限制 | 待上传清单标记“不支持或超出大小限制”。 | 移除该文件或重新选择合规文件。 |
| 上传中关闭弹窗 | 提示退出将中断剩余上传，已上传文件保留。 | 取消继续上传，或确认停止。 |
| 未选学科或学生 | 确认发布按钮不可用。 | 补齐两个必填项。 |
| 文件审核中 / 审核失败 | 不展示发布操作，提示暂不可发布。 | 等待审核完成或处理失败原因后重试。 |
| 删除发布 | 二次确认“删除后学生设备将不再显示该文件”。 | 取消保留发布；确认后同步移除。 |
| 当前目录无内容 | 文件列表展示空状态。 | 上传文件或进入其他目录。 |

## 8. 关联模块

| 模块 | 关系 |
| --- | --- |
| 我的资源 | 作为进入「我的文件」的上级入口。 |
| 墨水屏设备 | 接收教师发布的单个文件，并在删除发布后同步移除。 |
| 学生与班级 | 提供教学班 / 行政班维度的接收对象选择。 |
| 文件审核 | 向文件列表提供审核状态，并控制可否发布。 |

## 9. 过稿模式（Demo 辅助能力）

过稿模式不属于教师端业务功能，仅用于 Demo 评审。

- 正常演示模式不展示面板、编号标记或页面高亮。
- 通过常驻的「演示 / 过稿」切换进入过稿模式；切换不重载 Demo 页面。
- 过稿面板展示本次版本的评审范围和“定位页面”操作；编号标记与评审项一一对应。
- 产品稿链接在面板顶部统一提供，不在单个评审项中重复配置。
- 当前石墨产品稿链接待补充后配置。

## 10. QA 检查点

1. 根目录与任意文件夹均可进入上传；上传完成后文件出现在当前目录。
2. 文档 / 图片超过 100MB，音频 / 视频超过 300MB 时均不可上传，并展示对应限制。
3. 文件夹不出现“发布到墨水屏”；单个且审核通过的文件可发布。
4. 发布时未选择学科或学生，确认发布不可用。
5. 全选某班学生后，发布记录展示班级；只选部分学生后，展示学生。
6. 同一文件连续发布两次，生成两条独立记录；删除其中一条不影响另一条和教师端原文件。
7. 删除确认框展示「取消 / 确认」；确认后学生设备不再显示该次发布文件。
8. 文件详情页不展示发布记录，只展示发布到墨水屏与下载。
9. 审核中、审核失败文件不可发布，列表提示与操作状态一致。
10. 演示模式不显示过稿面板和标记；过稿模式可定位可见区域，退出后即时恢复演示模式且不重载页面。
